[] blindar todo nosso banco de dados em uma etapa de segurança usando o .env

[x] renomear: 
    [x]renomear classes para formateCase em inglês 
    [x]renomear as funcções do controller
    [x]renomear as funcções do tarefa.router
    [x]renomear as funcções do repositories

    renomear nomes das variaveis incluidas dentro do método e o método classe TarefaRepository:
        [x] método buscartodas by searchALL
        [x] método novaTarefa by newToDO
        [x] método atualizar by update
        [x] método deletar by remove

    renomear nomes das variaveis incluidas dentro do método e o método classe TarefaController:
        [x] método routeGet
        [X] método routePost
        [X] método routePut
        [X] método routeDelete

    renomear nomes das variaveis incluidas dentro do método e o método classe Tarefa.route:
        [x] método routeGet
        [] método routePost
        [] método routePut
        [] método routeDelete

    Delete: 
        [] arq. ToDoRepository duplicate with error
        [] arq. ToDoController duplicate with error  
        [] arq. ToDo.router duplicate with error 

    []renomear a classe TarefaRepository
    []renomear a classe TarefaControllers

    [x]renomear o arq. TarefaRepository p/ ToDO..
    [x]renomear o arq. TarefaControllers p/ ToDO..
    [x]renomear o arq. Tarefa.Router p/ ToDO..